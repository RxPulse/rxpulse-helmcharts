
global:
  namespace: rxpulse-dev
  environment: dev
  services:
    frontend:
      tag: v3
      replicaCount: 2
    user:
      tag: latest
      replicaCount: 1
    catalog:
      tag: latest
      replicaCount: 1
    inventory:
      tag: latest
      replicaCount: 1

catalog:
  seedJob:
    enabled: true
inventory:
  seedJob:
    enabled: true
frontend:
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 5
base:
  namespace: rxpulse-dev
  configMap:
    nodeEnv: development
mongodb:
  createStorageClass: true
  storage: 5Gi
  storageClass: nfs-client
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 1Gi
